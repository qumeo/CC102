#include <iostream>
using namespace std;

int main()
{
	int age;
	string parent;
	cout << "Enter age: "
	cin >> age;
	cout (age <= 13 (cout >> "With parents?", cin >> parent, parent == "yes" ? "G,PG": "G") age > 13 && < 16 (cout >> "With parents?", cin >> parent, parent == "yes" ? "G, PG, R" : "G,PG") : age > 16 ? "G, PG, R" : 0);
	float money;
	cout << (money >= 7.50 ? "Matinee" : money >= 10.50 ? "Evening" : 0);
	return 0;
}
